/**
 *
 * NCR 模块明细类型说明。
 * 当前 NCR 页面已经直接使用 ERP.Db 镜像类型 `DefectiveReworkOrderDetail`
 * （来源：`@/types/erp-db.generated`），不再额外定义 `NcrDetailItem` 包装类型。
 * 若后续需要扩展仅存在于前端的视图字段，请在对应 ViewModel 内单独定义类型，
 * 避免混淆持久化实体与纯 UI 结构。
 *
 */
export {}
